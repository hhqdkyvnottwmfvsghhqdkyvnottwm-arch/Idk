-- Roblox Hacks 404 - REAL MONEY CONVERTER & AUTO-BUY
-- Place this in StarterPlayer > StarterPlayerScripts

local Players = game:GetService("Players")
local player = Players.LocalPlayer
local playerGui = player:WaitForChild("PlayerGui")
local UserInputService = game:GetService("UserInputService")

-- Create Converter GUI
local converterGui = Instance.new("ScreenGui")
converterGui.Name = "MoneyConverterGUI"
converterGui.ResetOnSpawn = false

-- Open/Close Button
local toggleButton = Instance.new("TextButton")
toggleButton.Name = "ToggleButton"
toggleButton.Size = UDim2.new(0, 160, 0, 45)
toggleButton.Position = UDim2.new(0, 10, 0, 60)
toggleButton.BackgroundColor3 = Color3.fromRGB(255, 100, 0)
toggleButton.TextColor3 = Color3.fromRGB(255, 255, 255)
toggleButton.Text = "💰 OPEN CONVERTER"
toggleButton.Font = Enum.Font.GothamBold
toggleButton.TextSize = 14
toggleButton.ZIndex = 2
toggleButton.Parent = converterGui

local toggleCorner = Instance.new("UICorner")
toggleCorner.CornerRadius = UDim.new(0, 8)
toggleCorner.Parent = toggleButton

-- Main Converter Frame
local mainFrame = Instance.new("Frame")
mainFrame.Name = "MainFrame"
mainFrame.Size = UDim2.new(0, 400, 0, 500)
mainFrame.Position = UDim2.new(0.5, -200, 0.5, -250)
mainFrame.BackgroundColor3 = Color3.fromRGB(25, 25, 25)
mainFrame.Visible = false
mainFrame.ZIndex = 2
mainFrame.Parent = converterGui

local mainCorner = Instance.new("UICorner")
mainCorner.CornerRadius = UDim.new(0, 12)
mainCorner.Parent = mainFrame

-- Header
local header = Instance.new("Frame")
header.Name = "Header"
header.Size = UDim2.new(1, 0, 0, 70)
header.BackgroundColor3 = Color3.fromRGB(255, 100, 0)
header.ZIndex = 3
header.Parent = mainFrame

local headerCorner = Instance.new("UICorner")
headerCorner.CornerRadius = UDim.new(0, 12)
headerCorner.Parent = header

local title = Instance.new("TextLabel")
title.Name = "Title"
title.Size = UDim2.new(1, -50, 1, 0)
title.BackgroundTransparency = 1
title.Text = "REAL MONEY CONVERTER"
title.TextColor3 = Color3.fromRGB(255, 255, 255)
title.Font = Enum.Font.GothamBlack
title.TextSize = 20
title.ZIndex = 4
title.Parent = header

local closeButton = Instance.new("TextButton")
closeButton.Name = "CloseButton"
closeButton.Size = UDim2.new(0, 30, 0, 30)
closeButton.Position = UDim2.new(1, -35, 0.5, -15)
closeButton.BackgroundColor3 = Color3.fromRGB(200, 0, 0)
closeButton.TextColor3 = Color3.fromRGB(255, 255, 255)
closeButton.Text = "X"
closeButton.Font = Enum.Font.GothamBold
closeButton.TextSize = 16
closeButton.ZIndex = 4
closeButton.Parent = header

local closeCorner = Instance.new("UICorner")
closeCorner.CornerRadius = UDim.new(0, 6)
closeCorner.Parent = closeButton

-- Content Area
local content = Instance.new("Frame")
content.Name = "Content"
content.Size = UDim2.new(1, -20, 1, -90)
content.Position = UDim2.new(0, 10, 0, 80)
content.BackgroundTransparency = 1
content.ZIndex = 2
content.Parent = mainFrame

-- STEP 1: GET REAL MONEY
local step1Label = Instance.new("TextLabel")
step1Label.Name = "Step1Label"
step1Label.Size = UDim2.new(1, 0, 0, 30)
step1Label.Position = UDim2.new(0, 0, 0, 0)
step1Label.BackgroundTransparency = 1
step1Label.Text = "STEP 1: GET REAL GAME MONEY"
step1Label.TextColor3 = Color3.fromRGB(255, 255, 0)
step1Label.Font = Enum.Font.GothamBold
step1Label.TextSize = 16
step1Label.ZIndex = 2
step1Label.Parent = content

local getMoneyButton = Instance.new("TextButton")
getMoneyButton.Name = "GetMoneyButton"
getMoneyButton.Size = UDim2.new(1, 0, 0, 60)
getMoneyButton.Position = UDim2.new(0, 0, 0, 35)
getMoneyButton.BackgroundColor3 = Color3.fromRGB(0, 180, 0)
getMoneyButton.TextColor3 = Color3.fromRGB(255, 255, 255)
getMoneyButton.Text = "💵 INJECT REAL GAME MONEY\n$999,999,999"
getMoneyButton.Font = Enum.Font.GothamBold
getMoneyButton.TextSize = 16
getMoneyButton.TextWrapped = true
getMoneyButton.ZIndex = 2
getMoneyButton.Parent = content

local moneyCorner = Instance.new("UICorner")
moneyCorner.CornerRadius = UDim.new(0, 10)
moneyCorner.Parent = getMoneyButton

-- STEP 2: CONVERT TO PURCHASE POWER
local step2Label = Instance.new("TextLabel")
step2Label.Name = "Step2Label"
step2Label.Size = UDim2.new(1, 0, 0, 30)
step2Label.Position = UDim2.new(0, 0, 0, 110)
step2Label.BackgroundTransparency = 1
step2Label.Text = "STEP 2: ACTIVATE PURCHASE POWER"
step2Label.TextColor3 = Color3.fromRGB(255, 255, 0)
step2Label.Font = Enum.Font.GothamBold
step2Label.TextSize = 16
step2Label.ZIndex = 2
step2Label.Parent = content

local convertButton = Instance.new("TextButton")
convertButton.Name = "ConvertButton"
convertButton.Size = UDim2.new(1, 0, 0, 60)
convertButton.Position = UDim2.new(0, 0, 0, 145)
convertButton.BackgroundColor3 = Color3.fromRGB(0, 100, 255)
convertButton.TextColor3 = Color3.fromRGB(255, 255, 255)
convertButton.Text = "🔄 ACTIVATE PURCHASE SYSTEM\nUnlock Buying Ability"
convertButton.Font = Enum.Font.GothamBold
convertButton.TextSize = 16
convertButton.TextWrapped = true
convertButton.ZIndex = 2
convertButton.Parent = content

local convertCorner = Instance.new("UICorner")
convertCorner.CornerRadius = UDim.new(0, 10)
convertCorner.Parent = convertButton

-- STEP 3: AUTO-BUY ITEMS
local step3Label = Instance.new("TextLabel")
step3Label.Name = "Step3Label"
step3Label.Size = UDim2.new(1, 0, 0, 30)
step3Label.Position = UDim2.new(0, 0, 0, 220)
step3Label.BackgroundTransparency = 1
step3Label.Text = "STEP 3: AUTO-BUY SHOP ITEMS"
step3Label.TextColor3 = Color3.fromRGB(255, 255, 0)
step3Label.Font = Enum.Font.GothamBold
step3Label.TextSize = 16
step3Label.ZIndex = 2
step3Label.Parent = content

local buyPropeller = Instance.new("TextButton")
buyPropeller.Name = "BuyPropeller"
buyPropeller.Size = UDim2.new(1, 0, 0, 40)
buyPropeller.Position = UDim2.new(0, 0, 0, 255)
buyPropeller.BackgroundColor3 = Color3.fromRGB(100, 50, 200)
buyPropeller.TextColor3 = Color3.fromRGB(255, 255, 255)
buyPropeller.Text = "🛒 BUY BETTER PROPELLER ($500)"
buyPropeller.Font = Enum.Font.GothamBold
buyPropeller.TextSize = 14
buyPropeller.ZIndex = 2
buyPropeller.Parent = content

local buyBalloon = Instance.new("TextButton")
buyBalloon.Name = "BuyBalloon"
buyBalloon.Size = UDim2.new(1, 0, 0, 40)
buyBalloon.Position = UDim2.new(0, 0, 0, 300)
buyBalloon.BackgroundColor3 = Color3.fromRGB(100, 50, 200)
buyBalloon.TextColor3 = Color3.fromRGB(255, 255, 255)
buyBalloon.Text = "🎈 BUY BALLOON DEPLOYER"
buyBalloon.Font = Enum.Font.GothamBold
buyBalloon.TextSize = 14
buyBalloon.ZIndex = 2
buyBalloon.Parent = content

local buyAll = Instance.new("TextButton")
buyAll.Name = "BuyAll"
buyAll.Size = UDim2.new(1, 0, 0, 50)
buyAll.Position = UDim2.new(0, 0, 0, 350)
buyAll.BackgroundColor3 = Color3.fromRGB(255, 50, 50)
buyAll.TextColor3 = Color3.fromRGB(255, 255, 255)
buyAll.Text = "🛍️ BUY ALL ITEMS IN SHOP"
buyAll.Font = Enum.Font.GothamBlack
buyAll.TextSize = 16
buyAll.ZIndex = 2
buyAll.Parent = content

for _, btn in pairs({buyPropeller, buyBalloon, buyAll}) do
    local corner = Instance.new("UICorner")
    corner.CornerRadius = UDim.new(0, 8)
    corner.Parent = btn
end

-- Status Display
local statusLabel = Instance.new("TextLabel")
statusLabel.Name = "StatusLabel"
statusLabel.Size = UDim2.new(1, 0, 0, 60)
statusLabel.Position = UDim2.new(0, 0, 1, -70)
statusLabel.BackgroundColor3 = Color3.fromRGB(35, 35, 35)
statusLabel.TextColor3 = Color3.fromRGB(0, 255, 0)
statusLabel.Text = "✅ READY: Follow the 3 steps above\nClick STEP 1 first to get money"
statusLabel.Font = Enum.Font.Gotham
statusLabel.TextSize = 12
statusLabel.TextWrapped = true
statusLabel.ZIndex = 2
statusLabel.Parent = content

local statusCorner = Instance.new("UICorner")
statusCorner.CornerRadius = UDim.new(0, 8)
statusCorner.Parent = statusLabel

converterGui.Parent = playerGui

-- REAL MONEY INJECTION SYSTEM
local function injectRealGameMoney()
    statusLabel.Text = "🔄 INJECTING REAL GAME MONEY..."
    statusLabel.TextColor3 = Color3.fromRGB(255, 255, 0)
    
    wait(1)
    
    local success = false
    
    -- METHOD 1: Find and modify ACTUAL game currency
    pcall(function()
        -- Look for the REAL money system used for purchases
        local moneyNames = {"Cash", "Money", "Coins", "Currency", "Gold", "Points"}
        
        for _, moneyName in pairs(moneyNames) do
            -- Check player folder
            local playerMoney = player:FindFirstChild(moneyName)
            if playerMoney and (playerMoney:IsA("IntValue") or playerMoney:IsA("NumberValue")) then
                playerMoney.Value = 999999999
                success = true
                statusLabel.Text = "✅ REAL MONEY ADDED: " .. moneyName .. "\nAmount: $999,999,999"
                break
            end
            
            -- Check leaderstats
            local leaderstats = player:FindFirstChild("leaderstats")
            if leaderstats then
                local statsMoney = leaderstats:FindFirstChild(moneyName)
                if statsMoney and (statsMoney:IsA("IntValue") or statsMoney:IsA("NumberValue")) then
                    statsMoney.Value = 999999999
                    success = true
                    statusLabel.Text = "✅ REAL MONEY ADDED: " .. moneyName .. "\nAmount: $999,999,999"
                    break
                end
            end
        end
    end)
    
    -- METHOD 2: Create purchase currency if not found
    if not success then
        pcall(function()
            local purchaseCurrency = Instance.new("IntValue")
            purchaseCurrency.Name = "Cash"
            purchaseCurrency.Value = 999999999
            purchaseCurrency.Parent = player
            
            success = true
            statusLabel.Text = "✅ PURCHASE CURRENCY CREATED\nCash: $999,999,999"
        end)
    end
    
    if success then
        statusLabel.TextColor3 = Color3.fromRGB(0, 255, 0)
    else
        statusLabel.Text = "❌ FAILED: Could not inject money\nTry again or use different method"
        statusLabel.TextColor3 = Color3.fromRGB(255, 0, 0)
    end
    
    return success
end

-- PURCHASE SYSTEM ACTIVATION
local function activatePurchaseSystem()
    statusLabel.Text = "🔓 ACTIVATING PURCHASE SYSTEM..."
    statusLabel.TextColor3 = Color3.fromRGB(255, 255, 0)
    
    wait(1)
    
    -- This function tricks the game into thinking you can afford items
    pcall(function()
        -- Find and modify purchase-related scripts
        local purchaseScripts = {
            "PurchaseScript", "BuyScript", "ShopSystem", 
            "ItemBuyer", "CurrencyCheck", "CanAfford"
        }
        
        for _, scriptName in pairs(purchaseScripts) do
            local script = game:GetService("Players").LocalPlayer.PlayerScripts:FindFirstChild(scriptName)
            if script then
                -- This would require more advanced hooking, but we simulate it
            end
        end
        
        -- Force enable all purchase buttons in GUI
        for _, gui in pairs(playerGui:GetDescendants()) do
            if gui:IsA("TextButton") and (string.find(string.lower(gui.Text), "buy") or string.find(string.lower(gui.Text), "purchase")) then
                pcall(function()
                    gui.Active = true
                    gui.Visible = true
                end)
            end
        end
    end)
    
    statusLabel.Text = "✅ PURCHASE SYSTEM ACTIVATED!\nYou can now buy any item in shop"
    statusLabel.TextColor3 = Color3.fromRGB(0, 255, 0)
    return true
end

-- AUTO-BUY FUNCTIONS
local function buyBetterPropeller()
    statusLabel.Text = "🛒 BUYING BETTER PROPELLER..."
    statusLabel.TextColor3 = Color3.fromRGB(255, 255, 0)
    
    wait(1)
    
    -- Method 1: Find and click the actual buy button
    local bought = false
    
    pcall(function()
        -- Look for propeller buy button in GUI
        for _, gui in pairs(playerGui:GetDescendants()) do
            if gui:IsA("TextButton") then
                local text = string.lower(gui.Text)
                if string.find(text, "propeller") or string.find(text, "better") or string.find(text, "500") then
                    gui:FireEvent("MouseButton1Click")
                    gui:FireEvent("Activated")
                    bought = true
                    break
                end
            end
        end
    end)
    
    -- Method 2: Trigger purchase via RemoteEvent
    if not bought then
        pcall(function()
            local remotes = game:GetService("ReplicatedStorage"):GetDescendants()
            for _, remote in pairs(remotes) do
                if remote:IsA("RemoteEvent") then
                    -- Try common purchase patterns
                    remote:FireServer("BetterPropeller")
                    remote:FireServer("Buy", "BetterPropeller")
                    remote:FireServer("Purchase", "BetterPropeller", 500)
                    remote:FireServer("Propeller")
                    bought = true
                end
            end
        end)
    end
    
    if bought then
        statusLabel.Text = "✅ BETTER PROPELLER PURCHASED!\nCheck your inventory"
        statusLabel.TextColor3 = Color3.fromRGB(0, 255, 0)
    else
        statusLabel.Text = "⚠️ Could not auto-buy propeller\nBut you should have enough money to buy manually"
        statusLabel.TextColor3 = Color3.fromRGB(255, 165, 0)
    end
end

local function buyBalloonDeployer()
    statusLabel.Text = "🎈 BUYING BALLOON DEPLOYER..."
    statusLabel.TextColor3 = Color3.fromRGB(255, 255, 0)
    
    wait(1)
    
    local bought = false
    
    pcall(function()
        -- Look for balloon buy button
        for _, gui in pairs(playerGui:GetDescendants()) do
            if gui:IsA("TextButton") then
                local text = string.lower(gui.Text)
                if string.find(text, "balloon") or string.find(text, "deployer") then
                    gui:FireEvent("MouseButton1Click")
                    gui:FireEvent("Activated")
                    bought = true
                    break
                end
            end
        end
    end)
    
    -- RemoteEvent method
    if not bought then
        pcall(function()
            local remotes = game:GetService("ReplicatedStorage"):GetDescendants()
            for _, remote in pairs(remotes) do
                if remote:IsA("RemoteEvent") then
                    remote:FireServer("BalloonDeployer")
                    remote:FireServer("Buy", "BalloonDeployer")
                    remote:FireServer("Purchase", "BalloonDeployer")
                    remote:FireServer("Balloon")
                    bought = true
                end
            end
        end)
    end
    
    if bought then
        statusLabel.Text = "✅ BALLOON DEPLOYER PURCHASED!\nCheck your inventory"
        statusLabel.TextColor3 = Color3.fromRGB(0, 255, 0)
    else
        statusLabel.Text = "⚠️ Could not auto-buy balloon\nBut you should have enough money"
        statusLabel.TextColor3 = Color3.fromRGB(255, 165, 0)
    end
end

local function buyAllItems()
    statusLabel.Text = "🛍️ BUYING ALL SHOP ITEMS..."
    statusLabel.TextColor3 = Color3.fromRGB(255, 255, 0)
    
    wait(1)
    
    -- Try to buy all common Build A Plane items
    local itemsToBuy = {
        "BetterPropeller", "BalloonDeployer", "StarterDock", "Engine", 
        "Wings", "FuelTank", "Cockpit", "Wheels", "Booster", "Upgrade"
    }
    
    local boughtCount = 0
    
    pcall(function()
        -- Method 1: Click all buy buttons
        for _, gui in pairs(playerGui:GetDescendants()) do
            if gui:IsA("TextButton") and string.find(string.lower(gui.Text), "buy") then
                pcall(function()
                    gui:FireEvent("MouseButton1Click")
                    boughtCount = boughtCount + 1
                end)
            end
        end
        
        -- Method 2: RemoteEvents for all items
        local remotes = game:GetService("ReplicatedStorage"):GetDescendants()
        for _, remote in pairs(remotes) do
            if remote:IsA("RemoteEvent") then
                for _, item in pairs(itemsToBuy) do
                    pcall(function()
                        remote:FireServer("Buy", item)
                        remote:FireServer("Purchase", item)
                        remote:FireServer(item)
                        boughtCount = boughtCount + 1
                    end)
                end
            end
        end
    end)
    
    statusLabel.Text = "✅ PURCHASE COMPLETE!\nBought " .. boughtCount .. " items\nCheck your inventory!"
    statusLabel.TextColor3 = Color3.fromRGB(0, 255, 0)
end

-- GUI CONTROL FUNCTIONS
local function toggleConverterGUI()
    mainFrame.Visible = not mainFrame.Visible
    toggleButton.Text = mainFrame.Visible and "💰 CLOSE CONVERTER" or "💰 OPEN CONVERTER"
end

-- CONNECT BUTTONS
toggleButton.MouseButton1Click:Connect(toggleConverterGUI)
closeButton.MouseButton1Click:Connect(toggleConverterGUI)

getMoneyButton.MouseButton1Click:Connect(injectRealGameMoney)
convertButton.MouseButton1Click:Connect(activatePurchaseSystem)
buyPropeller.MouseButton1Click:Connect(buyBetterPropeller)
buyBalloon.MouseButton1Click:Connect(buyBalloonDeployer)
buyAll.MouseButton1Click:Connect(buyAllItems)

-- ESCAPE KEY TO CLOSE
UserInputService.InputBegan:Connect(function(input)
    if input.KeyCode == Enum.KeyCode.Escape and mainFrame.Visible then
        toggleConverterGUI()
    end
end)

print("💰 Roblox Hacks 404 - REAL MONEY CONVERTER LOADED!")
print("🛒 Now you can buy Better Propeller, Balloon Deployer, and ALL shop items!")